# Phase-C Stage-1 pilot — verdict (design §10; PI rulings §11)

**Verdict: PASS — production-ready pending Stage-2 authorization.** Two
arms generated by the committed `injection/gen_phaseC_resp.py`, GP-run
under the UNMODIFIED production config (`phaseC_resp_gl_v1.env`, jobs
56605518/56605519), scored by `injection/measure_phaseC_pairs.py` with
THE production matcher. Every number below is **PILOT ENGINEERING
VALIDATION** — none enters a production artifact; nothing here amends any
frozen criterion (§11: the pilot adjusted only pilot-adjustable items).

Arms: `prodlike` 208 injections (BAL-veto substrate, 5000 km/s truth-HCD
z-exclusion, veto rate 17.5% — the production generator redraws at this
measured rate); `clean` 102 injections (environment probe). 7 anchors
(19.6→22.0) × 3 z anchors × 3 SNR strata, 26–32 per anchor, all cells
populated. Finder tree: the primary worktree @ `1533333` (behavioral
identity with this branch verified — 3 docstring lines).

## The §11 checklist

| item | result |
|---|---|
| truth enters intended bins | ✓ exact anchor placement; balanced cells |
| spectra/catalogue rows generated correctly | ✓ trees GP-consumed; 576/248 rows; coadd consistency ≤ 5.9e-8 |
| forest/noise/continuum preserved | ✓ flux×T injection; native ivar untouched |
| matching unambiguous | ✓ 202/208 and 100/102 matched (1-to-1 greedy, dz_rel 0.01); join-collapse guard clean |
| multiple candidates handled | ✓ deterministic; rates 5.8% / 9.8% reported |
| response accounting closes | ✓ 208 = 200 op-matched + 2 sub-op + 6 unmatched (per-row, no drops) |
| support masks | PARTIAL — the labeled-artifact builder is Stage-2 work; schema frozen in the design |
| bridge anchors reproduce old support at pilot precision | ✓ NO anchor-cell with \|z_vs_old\| > 2 at n ≥ 5, EITHER arm |
| paired/shared covariance constructible | ✓ design-validated (C_old from stored rho spreads; healpix-split machinery); production-scale demonstration is Stage 2 |
| no clamp/stale artifact determines results | ✓ fresh dlacats; envelope used for comparison only |
| event yield realistic | ✓ 96% pairs/injection (sizing assumed ~90%) |
| variance assumptions realistic | ✓ measured dx sd 0.08–0.18 vs sizing's 0.14–0.21 (sizing conservative) |
| compute/storage credible | ✓ measured **106 CPU-s/spec** (prodlike; 76 clean) vs 167 planned; 8.3 CPU-h total; ~840 MB/6-healpix arm |
| preimage anchor design covers G3 contributors | ✓ (see observation below) |

Mechanical findings (pilot-repairable class): one SLURM step failed in
4 s and the next step covered its range (dlacats complete; the 15% retry
allowance is justified); the FP-side accounting of unmatched op-rows
needs the ANALYSIS-WINDOW cut before interpretation (373 unmatched
op-rows on prodlike include out-of-window and sub-floor-truth
detections by construction — a production-matcher refinement, not a
matching defect; 0 on clean after natural-truth attribution... 146
unmatched rows on clean are all out-of-window/sub-floor class since no
natural truth exists there).

## Pilot-precision observation (labeled, NOT confirmation)

The bridge region matches the old response everywhere, while at the
CLAMPED region the measured mean bias falls progressively below the
clamp's frozen top-anchor value: pooled dx mean +0.011 (21.2), −0.004
(21.6), **−0.050 (22.0)** against clamped predictions of +0.03…+0.09 —
a −0.03…−0.14 dex deviation, i.e. exactly the direction and magnitude
range the committed preimage identifies as capable of feeding the G3
deficit while relieving the 21.7+ over-prediction. Both arms agree. Per
§11 this is an engineering-validation observation: the production
campaign with its frozen holdout — not the pilot — decides it. No
criterion, anchor, or endpoint was changed in response.

## Updated production projection (pilot-adjustable items only)

2,533 injections × 106 CPU-s ≈ **75 CPU-h finder**; with generation,
retries and the already-spent pilot ≈ **≤ 110 CPU-h total response
campaign**; storage ≈ 6 GB (40 healpix). All frozen criteria (σ ≤ 116.7,
power ≥ 0.90, bridge tolerance) unchanged.
